# Troodon TypeScript SDK (skeleton)

Minimal v0 package: canonical JSON + read-only HTTP helpers.

```bash
npm install
npm run build
npm test
```

Signing / Ed25519 integration is intentionally left to the host app in this skeleton.
